import { RandomFox } from '@/features/random-fox';

import { globalStyles } from './styles';

export const App = () => {
  return (
    <div className={globalStyles}>
      <RandomFox />
    </div>
  );
};
